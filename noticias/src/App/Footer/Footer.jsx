import React from 'react';
import '../../Styles/App/Footer/footer.scss'

const Footer = () => {
    const footer = (
        <footer className="footer">

        </footer>
    );

    return footer;
}

export default Footer;